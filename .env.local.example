# ── Servidor (nunca expostos ao browser) ─────────────────────────────────────
# Webhook do n8n que chama o ZenDesk
N8N_WEBHOOK_URL=https://seu-n8n.dominio.com/webhook/XXXXX

# Credenciais ZenDesk (usadas pelo n8n)
ZD_SUBDOMAIN=mercadoe
ZD_EMAIL=usuario@mercadoe.com.br
ZD_TOKEN=token_zendesk_aqui

# Client Secret do Keycloak — fica apenas no servidor, nunca vai para o browser
SSO_CLIENT_SECRET=VJsOsLKEVVjpVG0aNVOrKTriTeluEuyu

# ── Cliente (prefixo NEXT_PUBLIC_ — aparecem no bundle do browser) ────────────
NEXT_PUBLIC_SSO_URL=https://trunk.sso.mercadoe.com
NEXT_PUBLIC_SSO_REALM=zerotrust
NEXT_PUBLIC_SSO_CLIENT_ID=me-zendesk-dashboard-recurring-issues
